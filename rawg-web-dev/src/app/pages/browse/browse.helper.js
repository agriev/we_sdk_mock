import paths from 'config/paths';

export default {
  itemsPath: {
    creators: paths.creator,
    developers: paths.developer,
    publishers: paths.publisher,
    tags: paths.tag,
    platforms: paths.platform,
    stores: paths.store,
    genres: paths.genre,
  },
};
